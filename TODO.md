- [ ] Update Render build commands to avoid `npm ci` when `client/package-lock.json` is missing
- [ ] Commit-friendly: ensure repo has correct lockfile strategy (prefer `npm install` for client build)
- [ ] Validate locally that the modified build command works (npm install + npm run build)
- [ ] (Optional) Add/commit `client/package-lock.json` if desired to re-enable `npm ci`

